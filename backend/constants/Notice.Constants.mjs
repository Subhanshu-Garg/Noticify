const PRIORITY_MAP = {
    HIGH: 'high',
    MEDIUM: 'medium',
    LOW: 'low'
}

const PRIORITY_ENUM = Object.values(PRIORITY_MAP)


const NOTICE = {
    PRIORITY: {
        MAP: PRIORITY_MAP,
        ENUM: PRIORITY_ENUM
    }
}

export default NOTICE